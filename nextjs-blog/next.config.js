/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    output: 'export',
}

 module.exports = nextConfig


/** The next.config.js file in Next.js is used to configure 
 * and customize the default behavior of a Next.js application. 
 * It allows 
 * developers to modify various settings to suit their needs, without changing the core Next.js code. */